# Frontend V1 Overview

> Legacy single-page bundle: each entry shows role and opening code context.
> Last updated: 2025-10-08

## `README.md`

- **Purpose:** Documentation note (first line: # Static Assets Directory)
- **Status:** Keep (Reference for legacy UI behavior).

---

## `api.js`

- **Purpose:** Legacy frontend JS module `api` (first line: // Clock skew detection and compensation system)
- **Status:** Keep (Reference for legacy UI behavior).

---

## `app.js`

- **Purpose:** Legacy frontend JS module `app` (first line: document.addEventListener('DOMContentLoaded', () => {)
- **Status:** Keep (Reference for legacy UI behavior).

---

## `auth.js`

- **Purpose:** Legacy frontend JS module `auth` (first line: document.addEventListener('DOMContentLoaded', () => {)
- **Status:** Keep (Reference for legacy UI behavior).

---

## `campaign-click-fix.css`

- **Purpose:** Legacy stylesheet `campaign-click-fix` (first rule: /* Campaign Click Fix - TASK-005a */)
- **Status:** Keep (Reference for legacy UI behavior).

---

## `css/inline-editor.css`

- **Purpose:** Legacy stylesheet `inline-editor` (first rule: /* Inline Editor Styles */)
- **Status:** Keep (Reference for legacy UI behavior).

---

## `css/pagination-styles.css`

- **Purpose:** Legacy stylesheet `pagination-styles` (first rule: /* Story Pagination Styles */)
- **Status:** Keep (Reference for legacy UI behavior).

---

## `index.html`

- **Purpose:** Static HTML entry point `index.html` (first line: <!doctype html>)
- **Status:** Keep (Reference for legacy UI behavior).

---

## `js/animation-helpers.js`

- **Purpose:** Legacy frontend JS module `animation-helpers` (first line: /**)
- **Status:** Keep (Reference for legacy UI behavior).

---

## `js/campaign-wizard.js`

- **Purpose:** Legacy frontend JS module `campaign-wizard` (first line: /**)
- **Status:** Keep (Reference for legacy UI behavior).

---

## `js/component-enhancer.js`

- **Purpose:** Legacy frontend JS module `component-enhancer` (first line: /**)
- **Status:** Keep (Reference for legacy UI behavior).

---

## `js/enhanced-search.js`

- **Purpose:** Legacy frontend JS module `enhanced-search` (first line: /**)
- **Status:** Keep (Reference for legacy UI behavior).

---

## `js/inline-editor.js`

- **Purpose:** Legacy frontend JS module `inline-editor` (first line: /**)
- **Status:** Keep (Reference for legacy UI behavior).

---

## `js/interface-manager.js`

- **Purpose:** Legacy frontend JS module `interface-manager` (first line: /**)
- **Status:** Keep (Reference for legacy UI behavior).

---

## `js/loading-messages.js`

- **Purpose:** Legacy frontend JS module `loading-messages` (first line: // Loading Messages Module - TASK-005b)
- **Status:** Keep (Reference for legacy UI behavior).

---

## `js/settings.js`

- **Purpose:** Legacy frontend JS module `settings` (first line: /**)
- **Status:** Keep (Reference for legacy UI behavior).

---

## `js/test_planning_block_parsing.js`

- **Purpose:** Legacy frontend JS module `test_planning_block_parsing` (first line: /**)
- **Status:** Keep (Reference for legacy UI behavior).

---

## `js/theme-manager.js`

- **Purpose:** Legacy frontend JS module `theme-manager` (first line: class ThemeManager {)
- **Status:** Keep (Reference for legacy UI behavior).

---

## `js/ui-utils.js`

- **Purpose:** Legacy frontend JS module `ui-utils` (first line: /**)
- **Status:** Keep (Reference for legacy UI behavior).

---

## `js/visual-validator.js`

- **Purpose:** Legacy frontend JS module `visual-validator` (first line: /**)
- **Status:** Keep (Reference for legacy UI behavior).

---

## `loading-messages.css`

- **Purpose:** Legacy stylesheet `loading-messages` (first rule: /* Loading Messages - TASK-005b */)
- **Status:** Keep (Reference for legacy UI behavior).

---

## `parallel_dual_pass.css`

- **Purpose:** Legacy stylesheet `parallel_dual_pass` (first rule: /**)
- **Status:** Keep (Reference for legacy UI behavior).

---

## `parallel_dual_pass.js`

- **Purpose:** Legacy frontend JS module `parallel_dual_pass` (first line: /**)
- **Status:** Keep (Reference for legacy UI behavior).

---

## `style.css`

- **Purpose:** Legacy stylesheet `style` (first rule: /* Remove: body { background-color: #f8f9fa; } since it's now handled by themes */)
- **Status:** Keep (Reference for legacy UI behavior).

---

## `styles/animations.css`

- **Purpose:** Legacy stylesheet `animations` (first rule: /* ==================================)
- **Status:** Keep (Reference for legacy UI behavior).

---

## `styles/bridge.css`

- **Purpose:** Legacy stylesheet `bridge` (first rule: /* CSS Variable Bridge - Maps old variables to new design system */)
- **Status:** Keep (Reference for legacy UI behavior).

---

## `styles/components.css`

- **Purpose:** Legacy stylesheet `components` (first rule: /* Modern Component Styles with Bootstrap Compatibility */)
- **Status:** Keep (Reference for legacy UI behavior).

---

## `styles/enhanced-components.css`

- **Purpose:** Legacy stylesheet `enhanced-components` (first rule: /* Enhanced Components CSS - Component Enhancement Milestone)
- **Status:** Keep (Reference for legacy UI behavior).

---

## `styles/globals.css`

- **Purpose:** Legacy stylesheet `globals` (first rule: /* Modern CSS Foundation - Figma Design System */)
- **Status:** Keep (Reference for legacy UI behavior).

---

## `styles/interactive-features.css`

- **Purpose:** Legacy stylesheet `interactive-features` (first rule: /* ==================================)
- **Status:** Keep (Reference for legacy UI behavior).

---

## `styles/planning-blocks.css`

- **Purpose:** Legacy stylesheet `planning-blocks` (first rule: /* Planning Block Choice Buttons Styling */)
- **Status:** Keep (Reference for legacy UI behavior).

---

## `themes/base.css`

- **Purpose:** Legacy stylesheet `base` (first rule: /* Core layout and structure - theme-agnostic */)
- **Status:** Keep (Reference for legacy UI behavior).

---

## `themes/cyberpunk.css`

- **Purpose:** Legacy stylesheet `cyberpunk` (first rule: /* Cyberpunk Theme */)
- **Status:** Keep (Reference for legacy UI behavior).

---

## `themes/dark.css`

- **Purpose:** Legacy stylesheet `dark` (first rule: /* Dark Theme */)
- **Status:** Keep (Reference for legacy UI behavior).

---

## `themes/fantasy.css`

- **Purpose:** Legacy stylesheet `fantasy` (first rule: /* Fantasy Theme */)
- **Status:** Keep (Reference for legacy UI behavior).

---

## `themes/light.css`

- **Purpose:** Legacy stylesheet `light` (first rule: /* Light Theme */)
- **Status:** Keep (Reference for legacy UI behavior).

---

