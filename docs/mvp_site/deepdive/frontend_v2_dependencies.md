# Frontend V2 Dependencies

> Last updated: 2025-10-08

The `node_modules/` directory contains ~1070 packages and should be reinstalled via `npm ci`.

- **Status:** Remove from version control; keep lockfile to rebuild.
