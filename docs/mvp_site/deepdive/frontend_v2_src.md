# Frontend V2 Source Overview

> Each entry lists the file role and the opening code context to aid TypeScript reimplementation.
> Last updated: 2025-10-08

## `App.tsx.unused`

- **Purpose:** Asset file `App.tsx.unused` (first line: import { useState, useEffect } from 'react')
- **Status:** Keep (Required for React V2 implementation).

---

## `AppWithRouter.tsx`

- **Purpose:** Code module `AppWithRouter.tsx` (first line: import { BrowserRouter as Router, Routes, Route, Navigate } from 'react-router-dom')
- **Status:** Keep (Required for React V2 implementation).

---

## `assets/figma-assets.ts`

- **Purpose:** Code module `figma-assets.ts` (first line: // Figma asset mappings)
- **Status:** Keep (Required for React V2 implementation).

---

## `components/CLAUDE.md`

- **Purpose:** Documentation markdown (first line: # CLAUDE.md - React Components Library)
- **Status:** Keep (Required for React V2 implementation).

---

## `components/CampaignCreation.tsx`

- **Purpose:** Reusable React component `CampaignCreation` (first line: 'use client')
- **Status:** Keep (Required for React V2 implementation).

---

## `components/CampaignCreationV2.tsx`

- **Purpose:** Reusable React component `CampaignCreationV2` (first line: import { useState, useEffect, useRef } from 'react')
- **Status:** Keep (Required for React V2 implementation).

---

## `components/CampaignList.tsx`

- **Purpose:** Reusable React component `CampaignList` (first line: import { useState, useEffect } from 'react')
- **Status:** Keep (Required for React V2 implementation).

---

## `components/CartPage.tsx`

- **Purpose:** Reusable React component `CartPage` (first line: 'use client')
- **Status:** Keep (Required for React V2 implementation).

---

## `components/Dashboard.tsx`

- **Purpose:** Reusable React component `Dashboard` (first line: 'use client')
- **Status:** Keep (Required for React V2 implementation).

---

## `components/ErrorBoundary.tsx`

- **Purpose:** Reusable React component `ErrorBoundary` (first line: import React, { Component, ErrorInfo, ReactNode } from 'react')
- **Status:** Keep (Required for React V2 implementation).

---

## `components/FeatureCards.tsx`

- **Purpose:** Reusable React component `FeatureCards` (first line: import { Crown, BookOpen, Globe } from 'lucide-react')
- **Status:** Keep (Required for React V2 implementation).

---

## `components/GamePlayView.tsx`

- **Purpose:** Reusable React component `GamePlayView` (first line: import { useState, useRef, useEffect } from 'react')
- **Status:** Keep (Required for React V2 implementation).

---

## `components/GameView.tsx`

- **Purpose:** Reusable React component `GameView` (first line: 'use client')
- **Status:** Keep (Required for React V2 implementation).

---

## `components/Header.tsx`

- **Purpose:** Reusable React component `Header` (first line: import { Badge } from './ui/badge')
- **Status:** Keep (Required for React V2 implementation).

---

## `components/HomePage.tsx`

- **Purpose:** Reusable React component `HomePage` (first line: 'use client')
- **Status:** Keep (Required for React V2 implementation).

---

## `components/LandingPage.tsx`

- **Purpose:** Reusable React component `LandingPage` (first line: 'use client')
- **Status:** Keep (Required for React V2 implementation).

---

## `components/MockModeToggle.tsx`

- **Purpose:** Reusable React component `MockModeToggle` (first line: /**)
- **Status:** Keep (Required for React V2 implementation).

---

## `components/ProductPage.tsx`

- **Purpose:** Reusable React component `ProductPage` (first line: 'use client')
- **Status:** Keep (Required for React V2 implementation).

---

## `components/SearchPage.tsx`

- **Purpose:** Reusable React component `SearchPage` (first line: 'use client')
- **Status:** Keep (Required for React V2 implementation).

---

## `components/SignupCarousel.tsx`

- **Purpose:** Reusable React component `SignupCarousel` (first line: 'use client')
- **Status:** Keep (Required for React V2 implementation).

---

## `components/SignupForm.tsx`

- **Purpose:** Reusable React component `SignupForm` (first line: 'use client')
- **Status:** Keep (Required for React V2 implementation).

---

## `components/ThemeSwitcher.tsx`

- **Purpose:** Reusable React component `ThemeSwitcher` (first line: 'use client')
- **Status:** Keep (Required for React V2 implementation).

---

## `components/figma/ImageWithFallback.tsx`

- **Purpose:** Code module `ImageWithFallback.tsx` (first line: import React, { useState } from 'react')
- **Status:** Keep (Required for React V2 implementation).

---

## `components/ui/accordion.tsx`

- **Purpose:** Code module `accordion.tsx` (first line: "use client";)
- **Status:** Keep (Required for React V2 implementation).

---

## `components/ui/alert-dialog.tsx`

- **Purpose:** Code module `alert-dialog.tsx` (first line: "use client";)
- **Status:** Keep (Required for React V2 implementation).

---

## `components/ui/alert.tsx`

- **Purpose:** Code module `alert.tsx` (first line: import * as React from "react";)
- **Status:** Keep (Required for React V2 implementation).

---

## `components/ui/aspect-ratio.tsx`

- **Purpose:** Code module `aspect-ratio.tsx` (first line: "use client";)
- **Status:** Keep (Required for React V2 implementation).

---

## `components/ui/avatar.tsx`

- **Purpose:** Code module `avatar.tsx` (first line: "use client";)
- **Status:** Keep (Required for React V2 implementation).

---

## `components/ui/badge.tsx`

- **Purpose:** Code module `badge.tsx` (first line: import * as React from "react";)
- **Status:** Keep (Required for React V2 implementation).

---

## `components/ui/breadcrumb.tsx`

- **Purpose:** Code module `breadcrumb.tsx` (first line: import * as React from "react";)
- **Status:** Keep (Required for React V2 implementation).

---

## `components/ui/button.tsx`

- **Purpose:** Code module `button.tsx` (first line: import * as React from "react";)
- **Status:** Keep (Required for React V2 implementation).

---

## `components/ui/calendar.tsx`

- **Purpose:** Code module `calendar.tsx` (first line: "use client";)
- **Status:** Keep (Required for React V2 implementation).

---

## `components/ui/card.tsx`

- **Purpose:** Code module `card.tsx` (first line: import * as React from "react";)
- **Status:** Keep (Required for React V2 implementation).

---

## `components/ui/carousel.tsx`

- **Purpose:** Code module `carousel.tsx` (first line: "use client";)
- **Status:** Keep (Required for React V2 implementation).

---

## `components/ui/chart.tsx`

- **Purpose:** Code module `chart.tsx` (first line: "use client";)
- **Status:** Keep (Required for React V2 implementation).

---

## `components/ui/checkbox.tsx`

- **Purpose:** Code module `checkbox.tsx` (first line: "use client";)
- **Status:** Keep (Required for React V2 implementation).

---

## `components/ui/collapsible.tsx`

- **Purpose:** Code module `collapsible.tsx` (first line: "use client";)
- **Status:** Keep (Required for React V2 implementation).

---

## `components/ui/command.tsx`

- **Purpose:** Code module `command.tsx` (first line: "use client";)
- **Status:** Keep (Required for React V2 implementation).

---

## `components/ui/context-menu.tsx`

- **Purpose:** Code module `context-menu.tsx` (first line: "use client";)
- **Status:** Keep (Required for React V2 implementation).

---

## `components/ui/dialog.tsx`

- **Purpose:** Code module `dialog.tsx` (first line: "use client";)
- **Status:** Keep (Required for React V2 implementation).

---

## `components/ui/drawer.tsx`

- **Purpose:** Code module `drawer.tsx` (first line: "use client";)
- **Status:** Keep (Required for React V2 implementation).

---

## `components/ui/dropdown-menu.tsx`

- **Purpose:** Code module `dropdown-menu.tsx` (first line: "use client";)
- **Status:** Keep (Required for React V2 implementation).

---

## `components/ui/form.tsx`

- **Purpose:** Code module `form.tsx` (first line: "use client";)
- **Status:** Keep (Required for React V2 implementation).

---

## `components/ui/hover-card.tsx`

- **Purpose:** Code module `hover-card.tsx` (first line: "use client";)
- **Status:** Keep (Required for React V2 implementation).

---

## `components/ui/input-otp.tsx`

- **Purpose:** Code module `input-otp.tsx` (first line: "use client";)
- **Status:** Keep (Required for React V2 implementation).

---

## `components/ui/input.tsx`

- **Purpose:** Code module `input.tsx` (first line: import * as React from "react";)
- **Status:** Keep (Required for React V2 implementation).

---

## `components/ui/label.tsx`

- **Purpose:** Code module `label.tsx` (first line: "use client";)
- **Status:** Keep (Required for React V2 implementation).

---

## `components/ui/menubar.tsx`

- **Purpose:** Code module `menubar.tsx` (first line: "use client";)
- **Status:** Keep (Required for React V2 implementation).

---

## `components/ui/navigation-menu.tsx`

- **Purpose:** Code module `navigation-menu.tsx` (first line: import * as React from "react";)
- **Status:** Keep (Required for React V2 implementation).

---

## `components/ui/pagination.tsx`

- **Purpose:** Code module `pagination.tsx` (first line: import * as React from "react";)
- **Status:** Keep (Required for React V2 implementation).

---

## `components/ui/popover.tsx`

- **Purpose:** Code module `popover.tsx` (first line: "use client";)
- **Status:** Keep (Required for React V2 implementation).

---

## `components/ui/progress.tsx`

- **Purpose:** Code module `progress.tsx` (first line: "use client";)
- **Status:** Keep (Required for React V2 implementation).

---

## `components/ui/radio-group.tsx`

- **Purpose:** Code module `radio-group.tsx` (first line: "use client";)
- **Status:** Keep (Required for React V2 implementation).

---

## `components/ui/resizable.tsx`

- **Purpose:** Code module `resizable.tsx` (first line: "use client";)
- **Status:** Keep (Required for React V2 implementation).

---

## `components/ui/scroll-area.tsx`

- **Purpose:** Code module `scroll-area.tsx` (first line: "use client";)
- **Status:** Keep (Required for React V2 implementation).

---

## `components/ui/select.tsx`

- **Purpose:** Code module `select.tsx` (first line: "use client";)
- **Status:** Keep (Required for React V2 implementation).

---

## `components/ui/separator.tsx`

- **Purpose:** Code module `separator.tsx` (first line: "use client";)
- **Status:** Keep (Required for React V2 implementation).

---

## `components/ui/sheet.tsx`

- **Purpose:** Code module `sheet.tsx` (first line: "use client";)
- **Status:** Keep (Required for React V2 implementation).

---

## `components/ui/sidebar.tsx`

- **Purpose:** Code module `sidebar.tsx` (first line: "use client";)
- **Status:** Keep (Required for React V2 implementation).

---

## `components/ui/skeleton.tsx`

- **Purpose:** Code module `skeleton.tsx` (first line: import { cn } from "./utils";)
- **Status:** Keep (Required for React V2 implementation).

---

## `components/ui/slider.tsx`

- **Purpose:** Code module `slider.tsx` (first line: "use client";)
- **Status:** Keep (Required for React V2 implementation).

---

## `components/ui/sonner.tsx`

- **Purpose:** Code module `sonner.tsx` (first line: "use client";)
- **Status:** Keep (Required for React V2 implementation).

---

## `components/ui/switch.tsx`

- **Purpose:** Code module `switch.tsx` (first line: "use client";)
- **Status:** Keep (Required for React V2 implementation).

---

## `components/ui/table.tsx`

- **Purpose:** Code module `table.tsx` (first line: "use client";)
- **Status:** Keep (Required for React V2 implementation).

---

## `components/ui/tabs.tsx`

- **Purpose:** Code module `tabs.tsx` (first line: "use client";)
- **Status:** Keep (Required for React V2 implementation).

---

## `components/ui/textarea.tsx`

- **Purpose:** Code module `textarea.tsx` (first line: import * as React from "react";)
- **Status:** Keep (Required for React V2 implementation).

---

## `components/ui/toggle-group.tsx`

- **Purpose:** Code module `toggle-group.tsx` (first line: "use client";)
- **Status:** Keep (Required for React V2 implementation).

---

## `components/ui/toggle.tsx`

- **Purpose:** Code module `toggle.tsx` (first line: "use client";)
- **Status:** Keep (Required for React V2 implementation).

---

## `components/ui/tooltip.tsx`

- **Purpose:** Code module `tooltip.tsx` (first line: "use client";)
- **Status:** Keep (Required for React V2 implementation).

---

## `components/ui/use-mobile.ts`

- **Purpose:** Code module `use-mobile.ts` (first line: import * as React from "react";)
- **Status:** Keep (Required for React V2 implementation).

---

## `components/ui/utils.ts`

- **Purpose:** Code module `utils.ts` (first line: import { clsx, type ClassValue } from "clsx";)
- **Status:** Keep (Required for React V2 implementation).

---

## `constants/campaignDescriptions.ts`

- **Purpose:** Constant definitions `campaignDescriptions` (first line: /**)
- **Status:** Keep (Required for React V2 implementation).

---

## `hooks/useAuth.tsx`

- **Purpose:** Custom React hook `useAuth` (first line: import { useState, useEffect } from 'react')
- **Status:** Keep (Required for React V2 implementation).

---

## `hooks/useTestMode.ts`

- **Purpose:** Custom React hook `useTestMode` (first line: import { useMemo } from 'react')
- **Status:** Keep (Required for React V2 implementation).

---

## `index.css`

- **Purpose:** Stylesheet for `index` (first rule: @tailwind base;)
- **Status:** Keep (Required for React V2 implementation).

---

## `lib/firebase.ts`

- **Purpose:** Library helper `firebase` (first line: import { initializeApp } from 'firebase/app')
- **Status:** Keep (Required for React V2 implementation).

---

## `pages/CampaignCreationPage.tsx`

- **Purpose:** React page component for CampaignCreationPage (first line: import { useState } from 'react')
- **Status:** Keep (Required for React V2 implementation).

---

## `pages/CampaignListPage.tsx`

- **Purpose:** React page component for CampaignListPage (first line: import { useState } from 'react')
- **Status:** Keep (Required for React V2 implementation).

---

## `pages/CampaignPage.tsx`

- **Purpose:** React page component for CampaignPage (first line: import { useEffect, useState } from 'react')
- **Status:** Keep (Required for React V2 implementation).

---

## `pages/LandingPage.tsx`

- **Purpose:** React page component for LandingPage (first line: import { useNavigate } from 'react-router-dom')
- **Status:** Keep (Required for React V2 implementation).

---

## `pages/SettingsPage.tsx`

- **Purpose:** React page component for SettingsPage (first line: import { useState, useEffect, useCallback, useRef } from 'react')
- **Status:** Keep (Required for React V2 implementation).

---

## `services/API_MISSING_ENDPOINTS.md`

- **Purpose:** Documentation markdown (first line: # Missing API Endpoints Documentation)
- **Status:** Keep (Required for React V2 implementation).

---

## `services/MOCK_DATA_README.md`

- **Purpose:** Documentation markdown (first line: # Frontend v2 Mock Data System)
- **Status:** Keep (Required for React V2 implementation).

---

## `services/README.md`

- **Purpose:** Documentation markdown (first line: # API Services Documentation)
- **Status:** Keep (Required for React V2 implementation).

---

## `services/api-with-mock.service.ts`

- **Purpose:** API/service helper `api-with-mock.service` (first line: /**)
- **Status:** Keep (Required for React V2 implementation).

---

## `services/api.service.ts`

- **Purpose:** API/service helper `api.service` (first line: /**)
- **Status:** Keep (Required for React V2 implementation).

---

## `services/api.types.ts`

- **Purpose:** API/service helper `api.types` (first line: /**)
- **Status:** Keep (Required for React V2 implementation).

---

## `services/campaignService.ts`

- **Purpose:** API/service helper `campaignService` (first line: /**)
- **Status:** Keep (Required for React V2 implementation).

---

## `services/index.ts`

- **Purpose:** API/service helper `index` (first line: /**)
- **Status:** Keep (Required for React V2 implementation).

---

## `services/mock-integration-example.tsx`

- **Purpose:** API/service helper `mock-integration-example` (first line: /**)
- **Status:** Keep (Required for React V2 implementation).

---

## `services/mock.service.ts`

- **Purpose:** API/service helper `mock.service` (first line: /**)
- **Status:** Keep (Required for React V2 implementation).

---

## `stores/README.md`

- **Purpose:** Documentation markdown (first line: # Zustand State Management for WorldArchitect.AI)
- **Status:** Keep (Required for React V2 implementation).

---

## `stores/authStore.ts`

- **Purpose:** Zustand store `authStore` (first line: import { create } from 'zustand';)
- **Status:** Keep (Required for React V2 implementation).

---

## `stores/campaignStore.ts`

- **Purpose:** Zustand store `campaignStore` (first line: import { create } from 'zustand';)
- **Status:** Keep (Required for React V2 implementation).

---

## `stores/gameStore.ts`

- **Purpose:** Zustand store `gameStore` (first line: import { create } from 'zustand';)
- **Status:** Keep (Required for React V2 implementation).

---

## `stores/index.ts`

- **Purpose:** Zustand store `index` (first line: // Barrel export for all stores)
- **Status:** Keep (Required for React V2 implementation).

---

## `stores/themeStore.ts`

- **Purpose:** Zustand store `themeStore` (first line: import { create } from 'zustand';)
- **Status:** Keep (Required for React V2 implementation).

---

## `styles/globals.css`

- **Purpose:** Stylesheet for `globals` (first rule: @tailwind base;)
- **Status:** Keep (Required for React V2 implementation).

---

## `test-real-api.ts`

- **Purpose:** Code module `test-real-api.ts` (first line: /**)
- **Status:** Keep (Required for React V2 implementation).

---

## `types/theme.ts`

- **Purpose:** Code module `theme.ts` (first line: export type Theme = 'light' | 'dark' | 'fantasy' | 'cyberpunk' | 'spooky';)
- **Status:** Keep (Required for React V2 implementation).

---

## `types.ts`

- **Purpose:** Code module `types.ts` (first line: // Re-export types from api.types for consistency)
- **Status:** Keep (Required for React V2 implementation).

---

## `utils/__tests__/errorHandling.security.test.ts`

- **Purpose:** Code module `errorHandling.security.test.ts` (first line: /**)
- **Status:** Keep (Required for React V2 implementation).

---

## `utils/dev.ts`

- **Purpose:** Utility helpers `dev` (first line: /**)
- **Status:** Keep (Required for React V2 implementation).

---

## `utils/errorHandling.ts`

- **Purpose:** Utility helpers `errorHandling` (first line: /**)
- **Status:** Keep (Required for React V2 implementation).

---

## `vite-env.d.ts`

- **Purpose:** Code module `vite-env.d.ts` (first line: /// <reference types="vite/client" />)
- **Status:** Keep (Required for React V2 implementation).

---

