---
description: Steer work on $USER's Ubuntu machine (jeff-ubuntu) via SSH at 192.168.254.128
type: prompt
---

Read ~/.claude/skills/linux-remote/SKILL.md then execute the user's request on jeff-ubuntu via SSH.

The connection is: `ssh jeff-ubuntu` (passwordless, alias configured in ~/.ssh/config).

Execute the task directly — do not hand commands back to the user to run manually.
