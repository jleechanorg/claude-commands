# Plan Command (Alias)

**Alias for**: `/execute`

**Usage**: `/plan` or `/execute` or `/e`

**Note**: ⚠️ This command has been consolidated into `/execute`. The `/plan` alias redirects to the full execute protocol which includes the same strategic planning phase.

See [execute.md](./execute.md) for full documentation.