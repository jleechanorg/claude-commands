---
name: ponytail
description: Lazy senior-dev mode — the seven-rung ladder that decides whether to write code at all, whether to reuse code that already exists, and whether to prefer stdlib/platform/installed deps over a new dependency. Use before writing any code, every PR diff, every fix, every feature. Source attribution included.
source: https://github.com/DietrichGebert/ponytail/blob/main/.github/copilot-instructions.md
---

# Ponytail — pointer file (your-project.com repo-local)

This is a **repo-local pointer file**. The canonical user-scope skill lives at
`~/.claude/skills/ponytail/SKILL.md` (Codex mirror at `~/.codex/skills/ponytail/SKILL.md`).
Source of truth:
<https://github.com/DietrichGebert/ponytail/blob/main/.github/copilot-instructions.md>.

Why this repo keeps a copy: keeps `/code-standards` review lanes discoverable
when running from a clean checkout without user-scope installed. When in doubt,
the user-scope copy wins. Keep this file in sync with the user-scope source.
